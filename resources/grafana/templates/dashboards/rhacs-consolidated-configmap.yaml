# rhacs-consolidated-configmap.yaml is generated from rhacs-central.json
# the configmap will be using RHOBS metrics and used in saas file app-interface
apiVersion: v1
kind: ConfigMap
metadata:
  creationTimestamp: null
  name: rhacs-consolidated
  labels:
    grafana_dashboard: "true"
  annotations:
    grafana-folder: /grafana-dashboard-definitions/Addons
data:
  json: |
